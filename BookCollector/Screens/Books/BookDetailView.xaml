<UserControl x:Class="BookCollector.Screens.Books.BookDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock FontSize="20" 
                       Text="{Binding Title}"
                       ToolTip="{Binding Title}"/>
            <TextBlock FontSize="16" Text="{Binding Authors}"/>
            <TextBlock Margin="0,10,0,0" Text="{Binding ISBN}"/>
            <TextBlock Text="{Binding ISBN13}"/>
            <TextBlock Margin="0,10,0,0"
                       Text="{Binding Description}"
                       TextWrapping="Wrap"/>
            <TextBlock Margin="0,10,0,0" Text="Tags"/>
            <DataGrid ItemsSource="{Binding Tags}"
                      Loaded="TagsLoaded"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
