<UserControl x:Class="BookCollector.Screens.Common.CollectionsNavigationPartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel MinWidth="150"
                Margin="16">
        <TextBlock Text="Collection"
                   Style="{StaticResource MaterialDesignSubheadingTextBlock}"/>
        <Button Margin="0,8,0,0"
                Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding ShowBooksCommand}"
                Content="Books"/>
        <Button Margin="0,8,0,0"
                Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding ShowSeriesCommand}"
                Content="Series"/>
        <Button Margin="0,8,0,0"
                Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding ShowNotesCommand}"
                Content="Notes"/>
    </StackPanel>
</UserControl>
